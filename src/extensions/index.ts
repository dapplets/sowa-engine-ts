import * as formatters from './formatters'
import * as views from './views'
import { EthereumExtension } from './ethereum'
import { SwarmExtension } from './swarm'

export {
    // ToDo: think about "Extension" naming
    EthereumExtension,
    SwarmExtension,
    formatters,
    views
}